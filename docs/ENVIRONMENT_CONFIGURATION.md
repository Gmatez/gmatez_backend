# Environment configuration (backend pointer)

Canonical copy lives in the Flutter repo (shared product docs):

`../gmatez-app/docs/ENVIRONMENT_CONFIGURATION.md`

Backend validation: `src/config/app-config.ts` (`loadConfig`).

Example local env: `.env.example`.
